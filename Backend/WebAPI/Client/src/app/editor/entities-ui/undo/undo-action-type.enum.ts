export enum UndoActionTypeEnum {
  textMutate = 1,
  deleteContent,
  reorder,
  mutateTitle,
  mutateTextMetadata,
  removeCollectionItems,
  restoreText,
  restoreCollection,
  restoreCollectionItems,
  mutateCollectionInfo,
}
