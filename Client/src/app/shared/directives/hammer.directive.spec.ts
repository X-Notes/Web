import { HammerDirective } from './hammer.directive';

describe('HammerDirective', () => {
  it('should create an instance', () => {
    const directive = new HammerDirective();
    expect(directive).toBeTruthy();
  });
});
